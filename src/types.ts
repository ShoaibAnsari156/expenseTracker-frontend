export type AllamountStatusType = {

}
export type Transaction = {
    title: string,
    category: string,
    date: string,
    amount: number
}
export type TrasactionListType = {
    date: string,
    title: string,
    category: string,
    amount: number
}